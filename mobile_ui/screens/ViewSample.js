import React from 'react';
import {Text, Button} from 'react-native';
import {ScreenContainer} from '../styles/screenContainer';

export const ViewSample = ({navigation}) => {
  return (
    <ScreenContainer>
      <Text>View Sample Screen</Text>
    </ScreenContainer>
  );
};
